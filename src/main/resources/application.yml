spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/calculodesalario
    username: calculodesalario_app
    password: calculodesalario123
  flyway:
    enabled: true
    locations: classpath:db/migration
  thymeleaf:
    cache: false

server:
  port: 8080

logging:
  level:
    org.flywaydb: DEBUG
